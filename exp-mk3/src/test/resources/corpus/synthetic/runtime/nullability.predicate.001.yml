id: semantic.nullability.predicate.001
phase: semantic
kind: invalid
source: 'items := [1]; if items?.all(@ -> true) then 1 else 0 endif'
coverage: [assignment, collection-literal, identifier, call, lambda, at-symbol, boolean-literal, if-classic, safe-navigation, int-literal, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_NULLABLE_PREDICATE_NOT_ALLOWED
