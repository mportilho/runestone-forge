id: semantic.nullability.assignment.001
phase: semantic
kind: invalid
source: 'x := m?.["a"]; x'
environment:
  maxMaterializedSize: 256
  maxFactorialInput: 32
  symbols:
    m:
      type: MAP_NUMBER
      default:
        a: {type: NUMBER, value: "1"}
      overwritePolicy: FIXED
coverage: [assignment, identifier, safe-navigation, subscript-key, string-literal, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_NULLABLE_ASSIGNMENT_NOT_ALLOWED
