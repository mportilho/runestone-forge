id: semantic.collection.sort-by-direction.001
phase: semantic
kind: invalid
source: 'items := [1]; items.sortBy(@ -> @, "up")'
coverage: [assignment, collection-literal, identifier, call, lambda, at-symbol, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_OPERATION_ARGUMENT_MISMATCH
