id: semantic.collection.avg-type.001
phase: semantic
kind: invalid
source: 'items := ["a"]; items.avg()'
environment:
  maxMaterializedSize: 256
  maxFactorialInput: 32
coverage: [assignment, collection-literal, string-literal, identifier, call, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_OPERATOR_TYPE_MISMATCH
