id: runtime.collection.sort-by-atomicity.001
phase: runtime
kind: invalid
source: 'items := [3, 2, 1]; items.sortBy(@ -> 10 / (@ - 2), "asc")'
coverage: [assignment, collection-literal, identifier, call, lambda, at-symbol, additive, multiplicative, diagnostic]
expected:
  runtimeError:
    type: ExpressionExecutionException
    messageContains: 'division by zero'
