id: semantic.collection.lambda-nullable.001
phase: semantic
kind: invalid
source: 'items := [1]; items.map(@ -> items?.sum())'
coverage: [assignment, collection-literal, identifier, call, lambda, at-symbol, safe-navigation, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_NULLABLE_ARGUMENT_NOT_ALLOWED
