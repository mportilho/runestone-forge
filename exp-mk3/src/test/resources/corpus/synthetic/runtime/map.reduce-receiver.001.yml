id: semantic.map.reduce-receiver.001
phase: semantic
kind: invalid
source: 'm.reduce(0, @ -> @.accumulator + @.item)'
environment:
  maxMaterializedSize: 256
  maxFactorialInput: 32
  symbols:
    m:
      type: MAP_NUMBER
      default:
        b: {type: NUMBER, value: "2"}
        A: {type: NUMBER, value: "1"}
      overwritePolicy: FIXED
coverage: [identifier, call, int-literal, lambda, at-symbol, property-access, additive, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_OPERATOR_TYPE_MISMATCH
