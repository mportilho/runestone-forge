id: semantic.map.sort-by-receiver.001
phase: semantic
kind: invalid
source: 'm.sortBy(@ -> @.v, "asc")'
environment:
  maxMaterializedSize: 256
  maxFactorialInput: 32
  symbols:
    m:
      type: MAP_NUMBER
      default:
        b: {type: NUMBER, value: "2"}
        A: {type: NUMBER, value: "1"}
      overwritePolicy: FIXED
coverage: [identifier, call, lambda, at-symbol, property-access, string-literal, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_OPERATOR_TYPE_MISMATCH
