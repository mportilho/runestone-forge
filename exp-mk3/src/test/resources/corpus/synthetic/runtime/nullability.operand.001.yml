id: semantic.nullability.operand.001
phase: semantic
kind: invalid
source: 'm?.["a"] + 1'
environment:
  maxMaterializedSize: 256
  maxFactorialInput: 32
  symbols:
    m:
      type: MAP_NUMBER
      default:
        a: {type: NUMBER, value: "1"}
      overwritePolicy: FIXED
coverage: [identifier, safe-navigation, subscript-key, string-literal, int-literal, additive, diagnostic]
expected:
  diagnostic:
    category: SEMANTIC
    code: SEMANTIC_NULLABLE_OPERAND_NOT_ALLOWED
