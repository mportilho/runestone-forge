package com.runestone.expeval.internal.navigation;

/** Built-in map projection operations invocable via the {@code ..keys()} / {@code ..values()} suffix. */
public enum MapProjectionKind {
    KEYS,
    VALUES
}
