package com.runestone.expeval.internal.navigation;

/** Built-in vector aggregation operations invocable via the {@code ..sum()}, {@code ..avg()}, etc. suffix. */
public enum VectorAggregationKind {
    SUM,
    AVG,
    MIN,
    MAX,
    COUNT
}
